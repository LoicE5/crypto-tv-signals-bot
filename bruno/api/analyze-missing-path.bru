meta {
  name: Analyze - Missing Path (error case)
  type: http
  seq: 5
}

post {
  url: {{baseUrl}}/api/analyze
  body: json
}

body:json {
  {
    "inverted": false
  }
}

tests {
  test("returns 400 when path is missing", function() {
    expect(res.status).to.equal(400)
  })

  test("error message is present", function() {
    expect(res.body.error).to.be.a("string")
  })
}
